# Add this to src/core/scanner_engine.py in _initialize_analyzers method
    def _initialize_analyzers(self):
        '''Initialize and register all analyzers'''
        # Register pattern-based scanner
        self.register_analyzer(PatternBasedScanner())
        
        # Register AST-based analyzer
        self.register_analyzer(ParserBasedAnalyzer())
        
        # Register AI-enhanced analyzer
        try:
            from src.analyzers.ai_analyzer import AIEnhancedAnalyzer
            self.register_analyzer(AIEnhancedAnalyzer())
        except Exception as e:
            print(f'⚠ AI analyzer not available: {e}')
        
        print(f'✓ Initialized {len(self.analyzers)} analyzers')
